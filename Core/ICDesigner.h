#pragma once
#include <qwidget.h>

class ICDesigner : public QWidget
{
	Q_OBJECT

public:
	explicit ICDesigner(QWidget* parent = nullptr);
	~ICDesigner() = default;
private:

public slots:

private slots:

private:
};

